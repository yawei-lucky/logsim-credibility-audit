# 停车指标机制留出挑战 001 — 执行前冻结

2026-09-21。在 `stopping_metric_blindspot_001/run002` 完成后提出并冻结；
此机制未用于 V2 的设计或前述参数网格。不改动 V2 或回头调阈值。
这不是第三方盲审，也不是外部现实验证。

## 干预和待检验主张

保留 `v=10 m/s`、两帧确认完成于 `0.2 s`、制动开始时延 `0.1 s`、
最大减速度 `5 m/s²`。区别仅为减速度由零线性增加到最大值所需的时间：
`r=0` 为原合同，`r=0.6 s` 为未在 V2 设计中使用的机制。
比较障碍净距固定为 `14 m`，不从运行结果选择。

- 冻结 V2 仍使用常减速度公式，不修改它以适配本挑战。
- 同时保留前次 `[0.1,0.3] s` 时延区间，检查它能否包住新边界。
  这个范围是设计合同，不是现实标定。
- 独立参考：在制动建立期积分 `a(t)=-b(t-t_start)/r`，此后 `a=-b`。
  限定 `v > br/2`，即建立期结束时还未停止。
  积分给出自由停车距离
  `d_r = v(c+L) + v²/(2b) + vr/2 - br²/24`。
- 另用小步长状态推进交叉检查，步长 `0.002/0.001/0.0005 s`；
  与解析参考的允许差为 `1e-4 m`。这是计算交叉检查，不是现实独立来源。

## 允许的解释

如果 V2 或时延区间漏包新边界，只能否定“V2 可直接跨制动力建立机制使用”
这一扩展主张；不否定原常减速度合同中的充分性命题，不归因于 HUGSIM。
本轮不为绕过反例而补一个新指标。需要的是把动力学合同列为明确的适用条件，
而不是用同合同留出通过率掩盖机制未知。
