# 智驾世界仿真器可信验证方法

> 状态：第一版可证伪方法骨架。它已经用仓库中的既有正、负和模糊案例做了
> 回溯资格审计，但尚未获得跨场景、跨接收方或现实闭环结果的外部资格。
>
> 本文件回答“如何判断一项有边界的可信主张”。它不是 HUGSIM 总评分表，也不
> 把日志复现、传感器一致性、任务级一致性和闭环结果可信性误写成四个串行阶段。

## 0. 第一版方法要解决什么

可信性不是“画面看起来真实”、HUGSIM 自己的语义/深度/评分正常，或者某个 AD
给出了一条看似合理的轨迹。第一版方法只允许回答条件化问题：

> 在声明的任务、场景范围、接收方、干预类型、不确定性范围和误差边界内，
> 仿真器是否保留了足以产生可信任务关系和下游结果的信息？

当前 HUGSIM 试用命题进一步收窄为：

> 在已审计的白天城市六相机场景和车辆冲突类干预中，固定 SparseDrive 是否能从
> HUGSIM 输入中获得方向正确、超过局部重复误差且对声明变化不轻易反转的关键
> 目标/冲突关系与规划响应？

这项命题目前不包含响应幅度符合现实、物理 TTC、真实碰撞概率、AD 安全或
HUGSIM 的一般测试域适用性。

## 1. 可信主张合同

每项主张必须先写成以下元组，缺少的字段保留为“未资格化”，不能由实验输出
倒推填写：

$$
\mathcal{C}=(S,T,\Omega,A,I,Y,\Theta,\varepsilon,R,Req)
$$

| 字段 | 含义 | 当前 HUGSIM 第一版示例 |
|---|---|---|
| `S` | 被评价的仿真器、版本和接口 | pinned HUGSIM 与已记录适配器 |
| `T` | 预期用途/驾驶任务 | 比较车辆冲突强弱及规划响应方向 |
| `Ω` | ODD、场景、速度和时间范围 | 已审计的白天城市局部片段；尚不能外推 |
| `A` | 目标 AD 或接收方 | pinned SparseDrive Stage2；不是现实真值 |
| `I` | 干预及保持量 | 车辆远近、横向关系、遮挡或速度；其余量明确冻结 |
| `Y` | 任务相关观测和结果 | 干预状态、可见支撑、风险偏序、规划模式/轨迹、闭环状态 |
| `Θ` | 合理不确定性集合 | 重复运行已局部观测；现实参数、渲染和接收方范围仍未资格化 |
| `ε` | 任务允许误差或决策边界 | 尚无外部资格；不得用当前结果事后定义 |
| `R` | 独立参考及证据依赖关系 | 部分真实 RGB/AD factual anchor；状态真值和现实后果仍不足 |
| `Req` | 此项主张必须通过的证据项 | 从 `G/F/Q/U/O` 中预先选择，未要求的项不能被暗示为已通过 |

### 1.1 四种允许的结论强度

结论强度从弱到强，但不是必须逐级通过的流水线：

1. **内部执行/因果一致性**：声明的干预确实发生，内部几何、时序和传播关系
   没有违反预声明规律；
2. **接收方事实等效性**：同一接收方在匹配真实与事实仿真输入上的差异落入已
   资格化任务边界；
3. **有边界反事实稳健性**：不存在精确现实副本时，任务关系在合格约束、参数和
   接收方变化中不反转；
4. **闭环后果可信性**：规划、控制和安全后果在声明范围内保留现实中的关键关系。

前一项的通过不能自动证明后一项；后一项看似合理也不能反向修复前层错误。

## 2. 证据网络，而不是单线自证

```text
独立规律 / 真实数据 / 标注 / 车辆与任务范围
             │
             ├──> 指标、接收方和误差边界资格
             │
场景身份 ─> 执行有效性 ─> 世界状态 ─> 传感器输入 ─> AD 输出 ─> 闭环后果
             │              │             │          │
             └──── 硬约束 ──┴── D_domain ─┴─ E_CF ───┘
                                      │
                         参数/重复/接收方敏感性 Θ
```

### 2.1 五个必要证据门

| Gate | 必须回答的问题 | 典型证据 | 失败时的处理 |
|---|---|---|---|
| `G0 身份与时域` | 是否是同一场景、输入、版本和完整有效未来 | 哈希、时间戳、完整未来 gate | 依赖该段的主张 `rejected` |
| `G1 干预执行` | 声明改变和保持不变量是否真的成立 | 独立重算的位姿、footprint、投影和时间关系 | 干预效应主张 `rejected` |
| `G2 测量资格` | 工具是否测到目标构念并能识别已知错误 | 正负对照、来源/计算独立性、误差边界 | 工具主张 `rejected` 或 `down-weighted` |
| `G3 接收方合同` | 输入、历史、标定、reset 和原生输出是否有效 | 真实数据运行、错序/错时负对照、重复性 | 合同违反则相关主张 `rejected`；仅外部资格不足则最高 `down-weighted` |
| `G4 状态与闭环传播` | 同一动作是否只执行一次，所有消费者是否共享同一状态 | 控制—状态—相机—碰撞—反馈链检查 | 闭环结果主张 `rejected` |

Gate 是必要条件，不是可信度得分。任何必需 gate 被有效反例否定，不能用其他
指标的高分抵消。

### 2.2 三种证据独立性

每条证据同时记录来源，避免把“换了一个计算脚本”误写成现实真值：

- `simulator-self`：HUGSIM 状态、semantic、depth 或 scorer；适合内部诊断；
- `computation-independent`：第三方代码重算 HUGSIM 状态；能查实现错误，但输入
  仍由 HUGSIM 声明；
- `source-independent`：真实观测、独立标注/传感器、物理规律或外部合格范围；
  才能提升现实适用性。

RGB、语义和深度即使共变，也可能共享同一渲染失效。多个同源 AD 也不能按简单
多数票当作相互独立证据。

## 3. 最小数学裁决

### 3.1 事实域差

对同一目标 AD 和匹配事实输入，定义任务输出差异：

$$
D_{domain}=d\!\left(Y_A^{real},Y_A^{sim,factual}\right)
$$

当任务允许误差 `ε_T` 有独立依据时，使用等效性逻辑：只有 `D_domain` 的预声明
置信区间完整落入 `[0, ε_T]`，才能接受该任务范围内的事实等效。仅仅“差异不
显著”不能证明等效。当前仓库只有局部实测域差，没有合格的 `ε_T`，因此只能
报告差异，不能裁决现实等效。

### 3.2 反事实效应

$$
E_{CF}=Y_A^{sim,intervention}-Y_A^{sim,factual}
$$

反事实不要求每个未观测未来都有现实副本，但至少要满足：

1. `G0–G3` 中该主张所需 gate 有效；
2. 干预满足预声明的几何、动力学、可见性和因果约束；
3. `E_CF` 的方向符合预声明任务关系；
4. 若声称效应幅度可分辨，效应区间必须与同管线重复误差区间分离；
5. 若声称现实任务稳健，结论还必须在来源独立资格化的 `Θ` 内不反转。

“效应大于重复误差”只排除了局部数值噪声，不能证明幅度符合现实。

### 3.3 稳健性

把任务关系先写成一个有方向的差值 `Δ(θ)`。例如，`Δ` 可以是“强冲突条件减去
弱冲突条件的规划净距”。令 `s_pre` 为预先声明的方向：当预期 `Δ` 应增大时取
`+1`，预期 `Δ` 应减小时取 `-1`。定义方向裕量：

$$
m(\theta)=s_{pre}\,\Delta(\theta)
$$

`m(θ)>0` 表示方向正确，`m(θ)=0` 表示没有分开，`m(θ)<0` 表示发生反转。若
`u(θ)` 是已资格化的重复、测量和参数不确定性上界，则整个设计范围的最差裕量为：

$$
m_{robust}=\inf_{\theta\in\Theta_{design}}
\left[m(\theta)-u(\theta)\right]
$$

- `m_robust > 0`：在声明设计范围内，方向始终正确且没有被不确定性淹没；
- 区间跨过 `0`：当前证据无法稳定区分，结论 `down-weighted`；
- 出现经确认的负裕量：拒绝“整个设计范围内都稳健”的具体主张，并记录反转
  发生的边界。

例如，在 18 个人工组合中有 17 个方向正确，只能报告“设计网格内 `17/18` 未
反转”。因为这 18 个点不是按现实交通分布随机抽样，不能写成“现实中有
`94.4%` 概率正确”。只有 `Θ` 的现实分布和抽样机制获得外部资格后，才能估计
稳定概率及其置信区间。

### 3.4 零失效怎样形成安全证据

仿真没有发现危险并非毫无价值，但它需要覆盖和统计前提。若 `N` 次试验可以近似
视为来自已资格化目标场景分布的独立样本，失效判据有效，而且观察到 `0` 次
失效，则失效概率 `p` 的单侧置信上界为：

$$
p_{upper}=1-\alpha^{1/N}
$$

取 `95%` 置信度，即 `α=0.05`，大样本下近似为：

$$
p_{upper}\approx\frac{3}{N}
$$

因此，零失效可以支持“在该合格分布和检测能力下，失效率低于某个统计上界”，
但不能证明所有道路、所有未知危险中的绝对安全。若场景重复、分布不代表现实、
仿真器漏掉关键机制或失效检测器不可靠，上述 `N` 不能当作有效独立样本数，公式
也不能直接使用。

### 3.5 三标签裁决

- `accepted`：所需 gate 有效，工具有资格，预声明关系/边界成立，结论严格限制
  在已覆盖范围；
- `down-weighted`：结果有信息，但来源依赖、覆盖不足、边界未资格化或接收方
  不确定性阻止更强结论；
- `rejected`：有效观测与主张冲突、已定位诊断使测量无效、能力未被实际测试，
  或主张范围超过证据。

每个 `rejected` 只拒绝命名主张；实验发现和诊断价值单独裁决。方法不产生加权
总分，也不允许一个后层结果抵消硬约束失败。

## 4. 方法本身怎样获得资格

在继续运行新场景前，先把方法当作被测对象。第一版使用已有实验进行回溯
资格审计，完整记录见：

```text
docs/runs/credibility_method_qualification_001.md
docs/runs/credibility_method_qualification_001.json
```

资格维度如下：

| 维度 | 判据 | 第一版结果 |
|---|---|---|
| 错误敏感性 | 能拒绝已知坐标、时域、控制/状态和不合格对照问题 | `accepted` |
| 层间区分力 | 能保留“像素正、任务负”和“接口正、结果负” | `accepted` |
| 有限正对照识别 | 能接受明显几何/遮挡端点且限制结论范围 | `accepted` |
| 缺测与模糊保留 | 不插补缺失、不用总分强行通过 | `accepted` |
| 稳定性资格 | 少量重复能排除数值噪声，但尚无跨场景统计范围 | `down-weighted` |
| 外部特异性/校准 | 尚无足够现实真值和任务允许误差证明通过者现实正确 | `rejected`（该强主张） |

因此第一版方法整体为 `down-weighted`：它已经具有诊断和证伪能力，可以指导
下一项实验；但还不能单独签发“仿真器适合作为现实 AD 测试域”的结论。

## 5. 现阶段的使用规则

1. 先写 `C` 元组和最强允许主张，再选指标；
2. 先过必要 gate，再解释接收方和闭环结果；
3. 阈值来自任务后果、真实统计、物理合同或外部资格，不能看完结果再定；
4. 没有现实对应物时，报告约束符合性和设计范围稳健性，不伪称唯一现实未来；
5. 仿真中经排除 artifact 后稳定复现的危险可形成 AD 缺陷证据；零失效只有在
   仿真适用性、场景分布、覆盖和失效判据获得资格后，才能形成有范围的统计安全
   证据，不能单独证明无条件安全；
6. 新实验必须填补证据网络中的一个明确缺口，而不是只增加相似场景数量。
