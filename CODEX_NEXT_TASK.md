# Codex Next Task — 从参考系统盲区到 HUGSIM 时序机制审计

> 顶层：**预测结构一致性、动作效应一致性、任务边界一致性**。
> 目的不是累计通过项，而是检验证据排除了哪些错误机制、还剩多大任务歧义。
> 主方法见 [CREDIBILITY_VALIDATION_METHOD.md](CREDIBILITY_VALIDATION_METHOD.md)。

## 本轮已完成（2026-09-21）

[停车任务指标盲区 001](docs/runs/stopping_metric_blindspot_001.md)：

- 一维合成参考中，同为 4/12 帧缺失，可能停车边界为 12–20 m；
  再匹配最长缺失段仍有同样的边界范围。
- 冻结控制/动力学合同后，确认时间可约束停车边界；7,824 个同合同留出组合
  覆盖成立，但不是现实概率或跨机制证明。
- 保留两个越界使用反例：执行器时延未知，以及制动力逐渐建立。
  后者保持确认/制动启动时刻相同，却使 13 m 点预测漏掉 2.925 m。
- 只读检查表明现有 SparseDrive 闭环记录不满足一维锁存制动合同。
  因此没有强行启动新 HUGSIM 道路实验，也没有修改车辆模型或重判旧结果。

## 下一项：只迁移时序机制问题，不搬用简化停车公式

1. 复用已有 HUGSIM 六相机输入与冻结 SparseDrive，选取一次原生计划明显改变的
   已有短窗口；只检查窗口所需输入与完整输出时域，不全面重查仓库。
2. 冻结两条时间线：输入采集/交付时刻，以及仿真状态推进/控制执行时刻。
   当前 runner 等待推理后才 `env.step`；墙钟推理耗时不是自动注入的物理延迟。
3. 先预注册**相同数量、不同时间排列的输入缺失/陈旧**对照，并明确是停发、
   显式缺测还是旧帧交付；不混用这些不同机制，不把图片黑屏等同传感器缺测。
4. 先检查固定 AD 的完整原生规划时序是否出现任务相关差异及重复上界。
   有响应不等于现实等效；没有合格控制与完整结果窗口时，不算停车/风险真值。
5. 只有任务边界及控制合同确实相容，才进入一次有独立几何后果判据的闭环。
   否则报告工具不适用的具体原因，不扩模型/场景，不以代理指标强行过关。

下一次执行前先收敛第 1–3 项的具体输入合同；这不是本轮已经完成的 AD 实验。
参考系统的“两帧确认、5 m/s² 常制动”不代表 SparseDrive 或 HUGSIM。

## 暂缓与保留

- compact-conflict task compatibility 002 的 geometry-only 待办暂缓，未执行；
  原方案保留于 Git `ecf031c:CODEX_NEXT_TASK.md`。
- scene-0041 opposing-path 的几何/传输正面证据和任务时域不足保留原裁决，
  不因本轮方法更新翻案。
- 不更换 HUGSIM，不引入第二 AD，不训练新世界模型；四层仅作证据归档。
